platform :ios, '10.3'

target 'facile' do
  use_frameworks!
  
  # Pods for facile
  pod 'FirebaseAnalytics', '~> 6.0'
  pod 'FirebaseCrashlytics', '~> 4.0'
  pod 'SWXMLHash', '~> 4.7.0'
  
  target 'facilescanTests' do
    inherit! :search_paths
  end
    
end
